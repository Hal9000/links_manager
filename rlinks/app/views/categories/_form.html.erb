<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
        <% category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<h2>Editing: <%= category.title %></h2>
<hr>

<table><tr>
<td valign=top>
    <%= form.label :name %>
</td>

<td valign=top>
  <div class="field">
    <%= form.text_field :name, size: 15 %>
  </div>
</td>
</tr>
<tr>

<td valign=top>
    <%= form.label :title %>
</td>

<td valign=top>
  <div class="field">
    <%= form.text_field :title, size: 35 %>
  </div>
</td>

</tr>
<tr>
<td valign=top>
    Description
</td>

<td valign=top>
  <div class="field">
    <%= form.text_area :desc, rows:4, cols:30 %>
  </div>
</td>
</tr> </table>

<hr>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
