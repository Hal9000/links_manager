<p id="notice"><%= notice %></p>

<h1>Categories</h1>

<br>
<%= link_to 'New category', new_category_path %>
<br>
<br>

<table>
  <thead>
    <tr>
      <th align=left>Title</th>
      <th colspan="3"></th>
      <th align=left>Description</th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.title %></td>
        <td><%= link_to(category) { image_tag "eye.png", size: 16 } %>
        <td><%= link_to(edit_category_path(tag)) { image_tag "pencil.png", size: 16 } %>
        <td><%= link_to(category, method: :delete, data: { confirm: 'Are you sure?' }) do
                 image_tag "redx.png", size: 16 end
            %>
        <td><%= category.desc %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New category', new_category_path %>
